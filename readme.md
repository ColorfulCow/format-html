#### format html code using javascript in browser

##### usage

```javascript
import formatHtml from 'path/to/htmlFormat.js'

const formatHtmlStr = formatHtml(html_source_code, indent_size, indent_character, max_char)
```
